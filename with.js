const names = ['John', 'Peter', 'Mary', 'James', 'Victoria', 'Paul', 'Alice'];

const updatesNames = names.with(3, ['Christine']);

console.info('names :', names);
console.info('updatesNames :', updatesNames);